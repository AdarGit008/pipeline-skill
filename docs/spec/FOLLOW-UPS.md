# Build-session follow-ups (from spec-session reviews, 2026-07-20)

Carry these into the build session; none block the build start.

1. **Baseline README sibling note** — SPEC.md §5 promises a cross-reference note in baseline-skill's README ("two lenses, not two competing copies"). That repo is outside this build; tracked here as a follow-up task on AdarGit008/baseline-skill.
2. **BR-02 DORA 2023 PDF** — citation is search-snippet-verified (label in the rule). Re-fetch the full PDF if the citation ever becomes load-bearing (severity upgrade, dispute).
3. **HOT-04 Rule Insights endpoint** — verify at build time whether a public REST endpoint exists for ruleset bypass insights; the cross-ref is behind a capability probe either way (`forge: not consulted` on absence). If none exists, demote to a documented manual step in REFERENCE.md.
4. **Templates + REFERENCE/GLOSSARY content** — `templates/hotfix.md`, `templates/incident-response.md`, `templates/runbook-rollback.md`, `templates/flag.json`, REFERENCE.md, GLOSSARY.md are referenced in the spec but authored at build time (from rule FIX fields + baseline's structure).
5. **NIST final-PDF quotes (CM-3, AC-3(10))** — verbatim-confirmed via csrc.nist.gov draft markup + csf.tools mirror (labels in rules); final PDF was not text-extractable in the research pipeline. Re-verify if NIST publishes an extractable revision.
6. **Flag registry zero-records behavior** — FLAG-02 WARNs `flag registry empty` when opt-in is active with zero records; mirror baseline's claims-field zero-records behavior at build time (check baseline `rules/claim.json`).

## From build chunk #1 (rules-as-data, 2026-07-21)

7. **ENV-04 profile — SPEC §6 count vs the rule's own severity.** RULES.md and SPEC §6 line 101 both describe ENV-04 identically to BR-03 ("warning at team · blocker at critical" via `severity_by_profile`). That is only coherent with `profile: team` — a critical-only rule can neither warn *at team* nor make its escalation map meaningful. So ENV-04 is encoded `profile: team`. But SPEC §6's activation **table** lists ENV-04 in the critical row (+10) and omits it from the team row (+18), which mis-bins it: the true engine tier split is **core 11 / team 19 / critical 9**, not 11/18/10. Family counts (the binding acceptance criterion) are unaffected — DESC3·BR6·CI6·ENV6·IAC6·FLAG3·RB4·HOT4·GOV1 = 39. **Reconcile the SPEC §6 table in a doc pass** (move ENV-04 into the team row, fix the +18/+10 labels to +19/+9), or explicitly re-decide ENV-04 as critical-only and drop its `severity_by_profile` (one-field change either way).
8. **Check-kind count — 15 new, not 10.** ARCHITECTURE §5.2's new-kinds table lists ten; RULES.md's per-rule CHECK prose needs five more that §5.2 folded into narrative: `release-marks` (BR-06 — tags∨releases∨release-workflow with the young-repo grace SKIP), `env-deploy-path` (ENV-02), `env-trigger` (ENV-03), `iac-state` (IAC-02 — backend∧no-tracked-state∧gitignore, multi-tool), `iac-secrets` (IAC-06 — deterministic signature tier + tfvars/gitignore, fail-if-any). Full evaluator inventory is **9 reused + 15 new = 24 used kinds** (registry has 25 incl. the parked `json-field`). Chunk #4's "9 reused + 10 new" title is a headline; sized to 24 in `src/evaluators.mjs`.

## From build chunk #3 (engine, 2026-07-21)

Engine ships an injected-evaluator seam: `runRules({..., evalCheck})`; `evalCheck(check, rule) -> {ok: true|false|null, detail, signoff?}`. No spec drift.

## From build chunk #4 (evaluators, 2026-07-21)

9. **RB-01 (a core blocker) vs a `docs`-typed repo.** RB-01 is `applies_to: all` + core blocker, but its rationale is deploy-scoped ("the rollback plan must exist before the deploy that needs it"). A `docs`-type repo (or a no-descriptor repo that auto-detects as `docs`) with a README lacking a rollback section therefore FAILs a blocker — even though docs don't deploy. CI-01/02/03 already exclude `docs` via `applies_to`; RB-01 does not. Confirmed live in chunk-4 testing (a bare-README repo exits 1 on RB-01 alone). **Decide:** narrow RB-01 `applies_to` to the deployable set `["service","app","infra"]` (matching CI-03), or accept that pipeline-skill treats every repo as delivery-shaped and a docs repo should declare `type: docs` + carry a one-line rollback note. Minor — a pure docs repo is an unusual target for a delivery checker — but it contradicts §5.3's "a no-descriptor repo is not a red build" for the docs-detected case.
10. ~~**jdg-health is schema+expiry only until chunk #5.**~~ **RESOLVED (build chunk #4, issue #5).** `src/jdg.mjs` now owns the full `expected_state`/`tripwire`/`review_by` evaluator (`evaluateJudgment` → verdict lattice ok<drifted<unresolvable<expired<tripped); GOV-01's `jdg-health` calls `ledgerHealth` over it in-check (drifted/unresolvable surface as detail notes, never fail; invalid/expired/tripped FAIL). The minimal ledger reader was factored out of `evaluators.mjs` into `jdg.mjs` (`loadJudgments`/`loadFlags` now `[{file,name,data,errors}]` with an id≠filename integrity check; `selectSignoffs` = newest valid sign-off per subject).

## From build chunk #4 (jdg ledger, 2026-07-21)

11. **`jdg new` has no secret-scrub gate (deliberate delta vs baseline).** Baseline's `jdg new` runs its `scrub.mjs` over the record content before writing, so a `reason` carrying a live secret is caught pre-commit (kept as a draft). pipeline-skill ships no scrub subsystem (not in ARCHITECTURE §2's src map), so `jdg new` writes the schema-valid record directly. Low risk — a judgment `reason` is prose the author types, not harvested config — but a future hardening could either (a) port a minimal scrub, or (b) document in REFERENCE.md that judgment `reason`/`subject` must not embed secrets. Flag for the chunk #7 docs pass.
12. ~~**Stale comment in `evaluators.mjs`.**~~ **RESOLVED (build chunk #5, issue #6).** The header line now reads "`--self-check (src/selfcheck.mjs)`".

## From build chunk #5 (CLIs + self-check, 2026-07-21)

13. **`src/selfcheck.mjs` added — a small delta vs ARCHITECTURE §2's src map** (which lists repo/config/validate/evaluators/engine/jdg/report but not selfcheck). Sanctioned by issue #6 ("the `--self-check` validator may live in `src/`") and mirrors baseline-skill's `src/selfcheck.mjs`. It enforces §3's structural laws (blocker⇒deterministic incl. `severity_by_profile`→blocker; manual⇒judgment and its inverse; FLAG-family uniform opt-in; plane/context/certainty metadata; unknown kind/type/profile/severity/category/requires; duplicate ids; orphan types/profiles) and prints the per-type coverage matrix. Verified: passes on the locked 39 rules (coverage confirms core11/team19/critical9 per follow-up #7; certainty 23/13/3), exits 1 naming each violation on an injected menagerie of 11 malformed rules.
14. **CLI clocks: `check.mjs` uses real `today`, `jdg` accepts `--today`.** `check.mjs`/`pipeline check` score against the real wall clock (no override flag — a score is about *now*); only `jdg` exposes `--today`/`--facts` for deterministic ledger evaluation. If the chunk #7 tests need a fixed-clock scorecard (e.g. a golden with dated sign-offs), add a test-only `--today` passthrough to `makeEvalCheck` then — not needed for the CLI surface itself.
